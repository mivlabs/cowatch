# Trained model artifacts — too large for git (latest.joblib exceeds
# GitHub's 100MB hard limit); ship these to prod separately (see README).
models/*.joblib

# MLflow local tracking store — regenerated by training runs, ~1GB.
mlruns/
